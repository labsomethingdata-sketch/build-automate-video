# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Variables (pueden contener datos sensibles) — el .example sí se commitea
*.tfvars
*.tfvars.json
!*.tfvars.example

# Overrides y config de CLI
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
